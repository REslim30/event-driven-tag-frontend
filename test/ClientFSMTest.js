import { ClientFSM } from '../src/ClientFSM';
let assert = require("assert");

describe('ClientFSM', function() {
  let client;

  beforeEach(function() {
    client = new ClientFSM();
  });

  it('Test', function() {
    
  });
});
